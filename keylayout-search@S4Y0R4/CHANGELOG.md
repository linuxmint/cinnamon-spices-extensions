## 1.0.0

* Initial release.
* Retypes menu queries into every enabled keyboard layout, using the live X keymap rather than a hardcoded table.
* Extra matches are ranked on the menu's own scale, so they appear after matches found in the layout that was typed in.
* Settings for minimum query length, result limit, which fields are searched, and whether to search other layouts at all when the menu already found something.
